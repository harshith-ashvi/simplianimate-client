export interface ASCIIEffectForm {
  screenResolution: string;
  resolution: number;
  isColored: boolean;
  symbols: string;
  imageUrl: string;
}

export interface PixelEffectFormType {
  screenResolution: string;
  resolution: number;
  shape: string;
  imageUrl: string;
}
